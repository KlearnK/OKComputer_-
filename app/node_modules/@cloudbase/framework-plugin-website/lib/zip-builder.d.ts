import { Builder } from '@cloudbase/framework-core';
interface ZipBuilderBuildOptions {
    name: string;
    localPath: string;
    zipFileName: string;
    ignore: string[];
}
interface ZipBuilderOptions {
    /**
     * 项目根目录的绝对路径
     */
    projectPath: string;
}
export declare class ZipBuilder extends Builder {
    constructor(options: ZipBuilderOptions);
    build(options: ZipBuilderBuildOptions[]): Promise<{
        zipFiles: {
            name: string;
            options: {};
            source: string;
            entry: string;
        }[];
    }>;
    zipFile(src: string, dest: string): Promise<unknown>;
    zipDir(src: string, dest: string, ignore?: string[]): Promise<unknown>;
}
export {};
//# sourceMappingURL=zip-builder.d.ts.map